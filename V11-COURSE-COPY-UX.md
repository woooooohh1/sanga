# v11 과정 편집 UX

## 변경 사항

### 1) 섹션 토글 정렬
- 섹션 번호/제목, 설명, 토글 버튼을 3열 구조로 정렬했습니다.
- 토글 버튼은 항상 섹션 헤더 오른쪽 끝에 고정됩니다.
- 모바일에서는 제목/토글이 첫 줄, 설명이 둘째 줄로 내려갑니다.

### 2) 새 과정 추가 시작 화면
`+ 과정 추가` 클릭 시 바로 빈 폼을 열지 않고 다음 중 하나를 선택합니다.

- 빈 과정으로 시작
- 기존 과정 복사

### 3) 기존 과정 복사
복사할 기존 과정을 선택하고 필요한 범위를 선택할 수 있습니다.

- 기본 정보
- 상세 소개 / 핵심 포인트
- 핵심 역량
- 커리큘럼
- 추천 대상
- 수강 혜택
- FAQ
- 최근 기수 운영정보

새 과정으로 복사할 때는 다음 값은 그대로 복제하지 않습니다.
- course id
- slug
- 기존 cohort id
- 공개 상태

공개 상태는 안전하게 `비공개`로 시작합니다.

### 4) 기존 과정 편집의 기수 재사용
기존에 추가된 `기수 데이터 재사용` 기능은 그대로 유지됩니다.
이는 같은 과정의 다음 기수를 만들 때 사용합니다.

- 새 과정 추가 > 기존 과정 복사 = 다른 새 과정을 만들 때
- 기존 과정 상세편집 > 기수 데이터 재사용 = 같은 과정의 새 기수를 만들 때

## DB 변경
없음. 추가 SQL 실행이 필요하지 않습니다.
