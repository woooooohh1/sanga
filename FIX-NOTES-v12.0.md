# FIX NOTES v12.0

## 상담신청 상세 팝업
- 상담 팝업 헤더를 고정 영역으로 분리했습니다.
- 신청자 요약/문의내용/상태/관리메모만 내부에서 세로 스크롤됩니다.
- 하단 `닫기 / 상담 기록 저장` 액션은 내부 스크롤 하단에 sticky로 유지됩니다.
- 팝업 내부 스크롤바를 얇은 라운드형으로 정리했습니다.

## 과정 상세 편집 - 교육기간
- 기존 자유입력식 `09.07 ~ 10.16` 필드를 시작일/종료일 캘린더 선택 UI로 변경했습니다.
- 선택값은 `YYYY-MM-DD`로 저장하고 화면에는 `YYYY. MM. DD.`로 표시합니다.
- 기존 `MM.DD ~ MM.DD` 데이터는 편집 시 현재 연도를 기준으로 자동 변환해 캘린더에 표시합니다.
- 저장 시 과정 `period` 값은 `YYYY.MM.DD ~ YYYY.MM.DD` 형태로 함께 생성해 기존 목록/상세페이지와 호환됩니다.

## 커스텀 캘린더
- 연도/월 헤더, 이전/다음 월 이동, 오늘, 지우기 기능을 추가했습니다.
- 오늘 날짜, 선택 날짜, 다른 달 날짜를 시각적으로 구분합니다.
- 데스크톱에서는 팝업 영역에 잘리지 않도록 viewport 기준 위치를 자동 보정합니다.
- 모바일에서는 하단 시트에 가까운 형태로 표시됩니다.
