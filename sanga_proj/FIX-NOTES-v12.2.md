# v12.2

- Fixed custom calendar header layout so the year suffix no longer wraps to a second line.
- Rebuilt year/month controls with stable flex sizing at desktop and mobile widths.
- Kept Sunday red / Saturday blue styling and increased calendar day/week label weight for scanability.
- Added a restrained typography hierarchy across admin modals: titles 800, section headings 700, labels 600, values/body 500.
- Increased consultation note readability with 15px text and 1.7 line-height without making every UI element bold.
- Bumped admin CSS/JS cache query to v12.2.
