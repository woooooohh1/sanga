# FIX NOTES v12.4

- 달력 연도 입력을 1900~2500년 선택 콤보로 변경
- 월 선택과 동일한 안정적인 select 레이아웃으로 변경
- 날짜 키보드 입력은 `YYYYMMDD` 또는 이미 포맷된 `YYYY-MM-DD`만 허용
- `20250901` 입력 시 `2025-09-01`로 즉시 자동 포맷
- 잘못된 날짜/형식은 브라우저 alert 대신 관리자 전용 메시지 모달 표시
- 메시지 확인 후 잘못 입력한 날짜 필드를 자동 선택
